import { CommentTimePipe } from './comment-time.pipe';

describe('CommentTimePipe', () => {
  it('create an instance', () => {
    const pipe = new CommentTimePipe();
    expect(pipe).toBeTruthy();
  });
});
