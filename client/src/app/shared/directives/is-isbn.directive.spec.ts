import { IsIsbnDirective } from './is-isbn.directive';

describe('IsIsbnDirective', () => {
  it('should create an instance', () => {
    const directive = new IsIsbnDirective();
    expect(directive).toBeTruthy();
  });
});
